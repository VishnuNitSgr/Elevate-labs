# Day 4 – Sorting and Limiting Data (SQL)

## 📌 Task Overview
This task focuses on understanding how to **sort query results** and **limit/paginate data** using SQL.  
The goal is to learn how databases return ordered results and how pagination works in real-world applications.

---

## 🛠 Tools Used
- **MySQL Workbench**


---

## 📚 Concepts Covered

1. Sorting data using `ORDER BY`
   - Ascending order (`ASC`)
   - Descending order (`DESC`)
2. Sorting using **multiple columns**
3. Limiting results using `LIMIT`
4. Combining `ORDER BY` with `WHERE`
5. Pagination using `LIMIT` and `OFFSET`
6. Performance impact of sorting
7. Leaderboard-style queries
8. Handling edge cases

---

## 📄 Files Included

| File Name | Description |
|---------|------------|
| `Day4.sql` | SQL script containing all queries related to sorting, limiting, and pagination |

---

## 🧪 Sample Scenarios Implemented

- Fetching top N records based on score
- Sorting records with same values using secondary columns
- Displaying paginated results (page-wise data)
- Filtering records before sorting
- Creating a leaderboard query

---
